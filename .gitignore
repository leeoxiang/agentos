node_modules
.next
out
# Anchored: a bare "build" matches any directory at any depth, which silently
# excluded app/build/ — the page existed locally, was never committed, and 404'd
# in production while working fine in dev.
/build
.env
.env.local
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

.vercel
.env*

# build output of packages/*
dist

# The source video is 95MB; only the compressed public/ copy is tracked.
*.mov
